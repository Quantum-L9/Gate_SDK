{
  "schema": "l9.gate_sdk.release_identity_ledger.v2",
  "distribution_name": "constellation-node-sdk",
  "import_name": "constellation_node_sdk",
  "package_version": "1.1.0",
  "protocol_version": "1.0",
  "python_requires": ">=3.12",
  "release_tag": "v1.1.0",
  "compatibility_channel": "v1",
  "consumer_contract": {
    "mode": "moving_major_tag",
    "required_ref": "v1",
    "forbid_ref_kinds": ["branch", "commit_sha", "fork"],
    "note": "Consumers declare the moving major channel in active dependency manifests and direct executable installs. A resolved commit belongs in a generated lockfile as reproducibility evidence, never in a manifest as the compatibility contract."
  },
  "delivery_branch": "main",
  "delivery_policy": "PR_only_no_direct_merge",
  "release_authority": {
    "workflow": ".github/workflows/release-publish.yml",
    "identity_gate": "verify-tag: validate_release_identity.py --release-gate <tag> (tag == ledger release_tag, HEAD == tag object)",
    "channel_promotion": "promote-channel: moves compatibility_channel onto the release object after every gate, then --verify-tag",
    "note": "The only workflow that publishes, releases, or moves the channel. A workflow_dispatch run is a rehearsal and has no path to any of the three."
  },
  "github_release_url": "https://github.com/Quantum-L9/Gate_SDK/releases/tag/v1.1.0",
  "github_release_status": "created",
  "artifacts": {
    "wheel": null,
    "sdist": null,
    "note": "No published artifact for 1.1.0; consumers install from the compatibility channel via git+https and hash-verify the resolved archive in their generated lock."
  },
  "agreement_rules": [
    "package_version must equal pyproject.toml [project].version",
    "release_tag must equal v{package_version} and must remain immutable once published",
    "compatibility_channel must equal v{major(package_version)}",
    "release_tag and compatibility_channel must resolve to the same released object",
    "consumers must declare compatibility_channel — never the exact release tag, a branch, or a commit sha",
    "a generated consumer lock may record the concrete resolved object; that object is execution evidence, not the compatibility contract",
    "a consumer lock whose resolved object differs from the current compatibility_channel target is stale and fails the networked release-identity check",
    "main, master, cryptoxdog/Gate_SDK, and arbitrary 40-character consumer refs are forbidden in active dependency surfaces"
  ],
  "provenance": {
    "authority": "none",
    "note": "Non-authoritative audit evidence. No validator — in this repository or in any consumer — may treat these values as a required dependency identity.",
    "superseded_consumer_pin_policy": "Schema v1 carried a consumer_pin object. It first required consumers to pin release_commit_sha 2b2f53a28a59bbfb2fa45f5eac32b722d802209a, then was amended on this branch to preferred=moving_major_tag while still storing a sha. Both are retired: under v2 the consumer contract is the channel alone, and no sha is normative.",
    "observed_channel_object": "e9f829f982110be13752da8f18c7a9692e8ed908",
    "observed_at": "2026-09-20T00:00:00+00:00"
  },
  "observed_at_build": "2026-09-20T00:00:00+00:00",
  "task_id": "gate-sdk-version-identity-v1"
}
